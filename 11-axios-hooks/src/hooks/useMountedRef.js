import React from 'react';

const useMountedRef = () => {
    const mountedRef = React.useRef(false);

    React.useEffect(() =>
    )

    return (
        <div>
            
        </div>
    );
};

export default useMountedRef;